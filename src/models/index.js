import UserModel from './user.model';
import PostModel from './post.model';
import WalletModel from './wallet.model';
import TokenTransactionModel from './token_transaction.model';

const models = {
  UserModel,
  PostModel,
  WalletModel,
  TokenTransactionModel,
};

export default models;
